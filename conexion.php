<?php
   //variables de conexion a la base de datos
   
   $host = "localhost";
   $user = "root";
   $clave = "";
   $bd = "renova";

   //Crear conexion
   $conexion = new mysqli($host, $user, $clave, $bd);

    ?>